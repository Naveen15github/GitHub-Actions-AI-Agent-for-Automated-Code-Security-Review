# ── Core agent framework ──────────────────────────────────────────────────────
langgraph>=0.2.0
langchain-core>=0.3.0

# ── HTTP client (used directly instead of OpenAI SDK for fine-grained control) ─
requests>=2.32.0

# ── Environment variable loading ──────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Data validation ───────────────────────────────────────────────────────────
pydantic>=2.7.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-mock>=3.14.0

# ── Code formatting (optional, used in CI linting step) ──────────────────────
black>=24.4.0
